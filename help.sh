color='\033[0;32m'
nc='\033[0m'

echo -e "\nYou can change your name on the greeting new session with editing the 'name' variable in the '.bash_profile' located on ~ (Home directory)"
echo -e "\nTo remove qxBash, cd to where you clone the repo, then type './reset.sh' and execute it!"
echo -e "Fork this project on Github! ${color}https://github.com/qwyzex/qxBash${nc}"
echo -e "\n## ${color}WISHED UPDATE FOR FUTURE${nc} ##"
echo -e "\n  *Echoing the custom aliases instead of replacing\n  *Removing only the custom aliases instead of deleting the file\n  *Including pre-set Git Bash SDK Theme"